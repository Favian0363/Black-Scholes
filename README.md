# Black‑Scholes Option Pricing (Optional GUI)

Lightweight Python project to compute Black–Scholes European option prices and sensitivities (Greeks), with visualization and a possible future front end using Streamlit (or Flask) frontend.

Stack: Python, NumPy, SciPy (scipy.stats), Matplotlib, Streamlit (or Flask + CSS)
